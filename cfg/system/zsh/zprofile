#
# /etc/zsh/zprofile
#
listop prepend PROFILES ${(%):-%x}
# TODO: not working, fix
#ensure -d \
#  $XDG_DATA_HOME $XDG_CONFIG_HOME $XDG_STATE_HOME $XDG_BIN_HOME $XDG_LIB_HOME \
#  $XDG_BIN_DIRS $XDG_LIB_DIRS $XDG_DATA_DIRS $XDG_CONFIG_DIRS $XDG_CACHE_HOME \
#  $XDG_RUNTIME_DIR \
#  $XDG_DESKTOP_DIR $XDG_DOWNLOAD_DIR $XDG_TEMPLATES_DIR $XDG_PUBLICSHARE_DIR \
#  $XDG_DOCUMENTS_DIR $XDG_MUSIC_DIR $XDG_PICTURES_DIR $XDG_VIDEOS_DIR \
#  $ZDOTDIR $ZCACHEDIR $ZDATADIR $ZSTATEDIR
emulate sh -c 'source /etc/profile'
